[build-system]
# Nothing fancy, same default behavior specified in
# https://github.com/pypa/sampleproject/pyproject.toml
requires = [
    "setuptools>=61.0.0",
    "wheel",
]
build-backend = "setuptools.build_meta"
